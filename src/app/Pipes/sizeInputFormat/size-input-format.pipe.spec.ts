import { SizeInputFormatPipe } from './size-input-format.pipe';

describe('SizeInputFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SizeInputFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
