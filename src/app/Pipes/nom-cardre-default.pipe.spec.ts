import { NomCardreDefaultPipe } from './nom-cardre-default.pipe';

describe('NomCardreDefaultPipe', () => {
  it('create an instance', () => {
    const pipe = new NomCardreDefaultPipe();
    expect(pipe).toBeTruthy();
  });
});
