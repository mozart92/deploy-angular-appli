import { TranslateContenuPipe } from './translate-contenu.pipe';

describe('TranslateContenuPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateContenuPipe();
    expect(pipe).toBeTruthy();
  });
});
